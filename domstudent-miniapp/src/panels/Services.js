import react from 'react';

import { Panel, Header, Group, Cell, Div, Avatar, View, SimpleCell, InfoRow } from '@vkontakte/vkui';

const Services = ({ id, servicesList, user }) => {


    return (
        <View id={id} activePanel={id}>
            <Panel id={id}>
                <Group>
                    
                </Group>
            </Panel>
        </View>
    )
}

export default Services;